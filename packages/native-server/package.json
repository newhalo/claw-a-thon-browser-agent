{
  "name": "mcpb-native-server-gateway",
  "version": "1.0.0",
  "description": "Authenticated public gateway for MCP-B native server",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "env-cmd .env.local node --watch server.json",
    "start": "node server.js --env-file .env"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.8.1",
  "dependencies": {
    "@mcp-b/native-server": "^1.0.47"
  },
  "license": "MIT",
  "devDependencies": {
    "env-cmd": "^11.0.0"
  }
}
